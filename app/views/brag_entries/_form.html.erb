<%= form_with(model: brag_entry) do |form| %>
  <% if brag_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brag_entry.errors.count, "error") %> prohibited this brag_entry from being saved:</h2>

      <ul>
        <% brag_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
